'use client';import {useEffect,useMemo,useState} from 'react';import Link from 'next/link';import {Shell} from '@/components/shell';import {TestSummary,getJson} from '@/lib/api';
export default function Predictions(){const [rows,setRows]=useState<TestSummary[]>([]),[q,setQ]=useState(''),[status,setStatus]=useState('All'),[err,setErr]=useState('');useEffect(()=>{getJson<TestSummary[]>('/api/v1/predictions?limit=200').then(setRows).catch(e=>setErr(e.message))},[]);const shown=useMemo(()=>rows.filter(r=>(status==='All'||r.status===status)&&r.test_name.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>b.flaky_probability-a.flaky_probability),[rows,q,status]);return <Shell><h1 className="text-4xl font-semibold tracking-tight">Predictions</h1><div className="my-6 flex flex-col gap-3 md:flex-row"><input className="rounded-full border px-5 py-3" placeholder="Search tests" value={q} onChange={e=>setQ(e.target.value)}/><select className="rounded-full border px-5 py-3" value={status} onChange={e=>setStatus(e.target.value)}>{['All','Stable','At Risk','Flaky'].map(s=><option key={s}>{s}</option>)}</select></div>{err&&<p className="text-red-600">{err}</p>}<div className="card overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr>{['test name','prediction','confidence','status','failure freq','duration','last failure','framework'].map(h=><th className="p-4" key={h}>{h}</th>)}</tr></thead><tbody>{shown.map(r=><tr className="border-t hover:bg-slate-50" key={r.id}><td className="p-4 font-medium"><Link href={`/tests/${r.id}`}>{r.test_name}</Link></td><td className="p-4">{r.prediction}</td><td className="p-4">{Math.round(r.confidence*100)}%</td><td className="p-4">{r.status}</td><td className="p-4">{r.failure_frequency}</td><td className="p-4">{r.duration}s</td><td className="p-4">{r.last_failure?new Date(r.last_failure).toLocaleDateString():'—'}</td><td className="p-4">{r.framework}</td></tr>)}</tbody></table></div></Shell>}

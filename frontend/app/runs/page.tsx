'use client';import {useEffect,useState} from 'react';import {Shell} from '@/components/shell';import {Run,getJson} from '@/lib/api';
export default function Runs(){const [rows,setRows]=useState<Run[]>([]),[err,setErr]=useState('');useEffect(()=>{getJson<Run[]>('/api/v1/tests/runs').then(setRows).catch(e=>setErr(e.message))},[]);return <Shell><h1 className="text-4xl font-semibold tracking-tight">Test runs</h1>{err&&<p className="text-red-600">{err}</p>}<div className="card mt-8 overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 muted"><tr>{['run','timestamp','total tests','passed','failed','flaky','duration','reliability'].map(h=><th className="p-4" key={h}>{h}</th>)}</tr></thead><tbody>{rows.map(r=><tr className="border-t" key={r.id}><td className="p-4 font-medium">{r.id}</td><td className="p-4">{new Date(r.timestamp).toLocaleString()}</td><td className="p-4">{r.total_tests}</td><td className="p-4">{r.passed}</td><td className="p-4">{r.failed}</td><td className="p-4">{r.flaky}</td><td className="p-4">{r.duration}s</td><td className="p-4">{r.reliability_score}%</td></tr>)}</tbody></table></div></Shell>}

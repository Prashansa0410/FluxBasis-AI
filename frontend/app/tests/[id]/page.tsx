'use client';import {useEffect,useState} from 'react';import {useParams} from 'next/navigation';import {BarChart,Bar,ResponsiveContainer,XAxis,YAxis,Tooltip} from 'recharts';import {Shell,Stat} from '@/components/shell';import {TestSummary,getJson} from '@/lib/api';
export default function Detail(){const {id}=useParams();const [t,setT]=useState<TestSummary|null>(null),[err,setErr]=useState('');useEffect(()=>{getJson<TestSummary>(`/api/v1/tests/${id}`).then(setT).catch(e=>setErr(e.message))},[id]);return <Shell>{err&&<p className="text-red-600">{err}</p>}{!t&&!err&&<div className="card h-80 animate-pulse"/>}{t&&<><h1 className="text-4xl font-semibold tracking-tight">{t.test_name}</h1><div className="mt-8 grid gap-4 md:grid-cols-4"><Stat label="Prediction" value={t.status}/><Stat label="Confidence" value={`${Math.round(t.confidence*100)}%`}/><Stat label="Risk level" value={t.risk_level}/><Stat label="Duration" value={`${t.duration}s`}/></div><div className="mt-8 grid gap-6 lg:grid-cols-2"><div className="card p-6"><h2 className="font-medium">SHAP feature importance</h2><ResponsiveContainer height={300}><BarChart data={t.shap_values.slice(0,6)} layout="vertical"><XAxis type="number"/><YAxis dataKey="feature" type="category" width={120}/><Tooltip/><Bar dataKey="importance" fill="#2563eb" radius={[0,8,8,0]}/></BarChart></ResponsiveContainer></div><div className="card p-6"><h2 className="font-medium">Prediction explanation</h2><p className="mt-4 muted">{t.risk_factors[0] || 'The model did not surface strong risk factors for this test.'}</p><h3 className="mt-8 font-medium">Recommended action</h3><p className="mt-3 muted">{t.recommended_action || 'No immediate action required from the available data.'}</p>{t.risk_factors.map(f=><p className="mt-3 rounded-2xl bg-slate-50 p-4 text-sm" key={f}>{f}</p>)}</div></div></>}</Shell>}
